import { MarkdownLayout } from "../../components/MarkdownLayout";
import { AshbyEmbed } from "../../components/AshbyEmbed";

export const meta = {
  title: "Jobs",
  titleClass: "ashby-margin-left",
};

<h1 class="ashby-margin-left">Work at PrairieLearn</h1>

<AshbyEmbed />

export default ({ children }) => (
  <MarkdownLayout meta={meta}>{children}</MarkdownLayout>
);
